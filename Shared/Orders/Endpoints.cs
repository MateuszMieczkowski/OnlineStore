namespace OnlineStore.Shared.Orders;

// TODO remove class after implementating
public class Endpoints
{
    // POST: /orders

    
    // GET: /orders - zwrócenie zamówień po userId wziętego z http contextu
    // OrderStatuses - statusy odzielone średnikami np. Created;Proccessing

    // GET /orders/all - zwrócenie wszystkich zamówień - dla admina
    // UserId - jeśli podany -> tylko zamówienia danego usera, jeśli nie podany -> wszystkie zamówienia

    // PUT /orders/{orderId}/cancel - tylko Admin

    // PUT /orders/{orderId}/complete - tylko Admin

    // GET /orders/addresses

    // POST /orders/addresses
}
